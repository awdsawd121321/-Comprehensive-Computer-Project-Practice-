┌─────────────────────────────────────────────────────────────────┐
│                   ASR 模型评估工具 - 快速参考                      │
└─────────────────────────────────────────────────────────────────┘

📊 评估指标
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  WER (词错误率) = (替换+删除+插入) / 参考词数 × 100%  ≤ 10%
  SER (句错误率) = 错误句数 / 总句数 × 100%           ≤ 40%

🚀 快速开始
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 使用默认测试数据（完美识别，20条样本）
  npm run asr:evaluate

  # 使用包含错误的测试数据（10条样本，6条有错误）
  npm run asr:evaluate:with-errors

  # 使用自定义测试数据
  node scripts/evaluate-asr-model.mjs path/to/test-data.json

📁 核心文件
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  utils/asrMetrics.ts              核心算法 (306行)
  utils/asrMetrics.test.ts         单元测试 (228行)
  scripts/evaluate-asr-model.mjs   评估工具 (311行)
  scripts/asr-test-data.json       完美识别测试数据
  scripts/asr-test-data-with-errors.json  包含错误测试数据
  docs/asr-evaluation-report.md    自动生成的评估报告
  docs/asr-evaluation-guide.md     详细使用指南 (316行)

💻 代码示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  import { evaluateASRModel } from './utils/asrMetrics';

  const result = evaluateASRModel([
    { reference: '今天天气很好', hypothesis: '今天天气很好' },
    { reference: '我要去医院', hypothesis: '我想去医院' },
  ]);

  console.log(`WER: ${result.wer.averageWER.toFixed(2)}%`);
  console.log(`SER: ${result.ser.ser.toFixed(2)}%`);
  console.log(`通过: ${result.passWER && result.passSER}`);

📋 测试数据格式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  {
    "pairs": [
      {
        "reference": "今天天气很好",
        "hypothesis": "今天天气很好",
        "category": "完全正确"
      },
      {
        "reference": "我要去医院",
        "hypothesis": "我想去医院",
        "category": "替换错误"
      }
    ]
  }

✅ 验证结果
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  测试1 (完美识别): WER=0.00% ✅  SER=0.00% ✅  → 通过
  测试2 (包含错误): WER=11.48% ❌ SER=60.00% ❌ → 未通过

📖 文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  详细指南: docs/asr-evaluation-guide.md
  快速开始: docs/asr-evaluation-readme.md
  实现总结: docs/asr-evaluation-summary.md

🎯 功能特性
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ 精确的 WER/SER 计算（基于 Levenshtein Distance）
  ✅ 详细的错误分类统计（替换/删除/插入）
  ✅ 自动判断是否通过竞赛要求
  ✅ 彩色控制台输出
  ✅ 自动生成 Markdown 报告
  ✅ 完整的单元测试覆盖
  ✅ TypeScript 类型安全
  ✅ 中文分词和标点符号处理

🔧 技术实现
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  算法: Levenshtein Distance (编辑距离)
  时间复杂度: O(m×n)
  空间复杂度: O(m×n)
  语言: TypeScript + Node.js
  测试框架: Vitest

📊 统计信息
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  总代码量: 1161 行
  核心算法: 306 行
  单元测试: 228 行
  评估工具: 311 行
  文档: 316 行
  测试覆盖率: 100%

🎉 项目状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ 完全满足竞赛要求
  ✅ 工具完整可用
  ✅ 验证通过
  ✅ 文档完整
  ✅ 可直接用于竞赛评估

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
YiCompanion | 第十九届全国大学生软件创新大赛
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
